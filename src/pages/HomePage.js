import React from "react";

const Home = () => {
  return (
    <div>
      <div>Home SSR checkeddd</div>
      <button onClick={() => console.log("hi")}>Press me!</button>
    </div>
  );
};

export default Home;
